<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for the admin section. -->
    <style name="Base.Theme.Plantify_admin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Make the app fullscreen and allow the navigation bar to be transparent -->
        <item name="android:windowFullscreen">true</item> <!-- Enable fullscreen mode -->
        <item name="android:windowTranslucentNavigation">true</item> <!-- Make the navigation bar transparent -->
    </style>

    <style name="Theme.Plantify_admin" parent="Base.Theme.Plantify_admin" />
</resources>
